<?php

/**
 * Implements hook_theme().
 */
function dependency_inject_services_theme($existing, $type, $theme, $path) {
  return [
    'my_template' => [
      'variables' => ['data' => NULL],
    ],
  ];
}